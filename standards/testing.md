# Testing Standards

- Add or update tests for behavior changes.
- Prefer targeted tests during iteration, broader checks before handoff.
- Cover success paths, edge cases, and failure/authorization cases.
- Keep tests deterministic and avoid hidden external dependencies.
- Never claim tests passed unless they were run and observed.
